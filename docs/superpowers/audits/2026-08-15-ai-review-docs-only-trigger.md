# AI review docs-only verification trigger

Verification-only artifact. This change exists solely to prove that the dual-model review workflow recognizes a Pull Request whose complete diff is documentation-only and skips both paid provider jobs while still publishing deterministic metadata through the prepare/post-comment path.

No runtime, methodology, state, schema, PWA, backend, or product behavior is changed by this file.
